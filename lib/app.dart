import 'package:flutter/material.dart';
import 'package:flutter_bmm_calculator/screen/all.dart';

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) => new MaterialApp(
        home: new HomeScreen(),
      );
}
